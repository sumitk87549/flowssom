import Svg,{Circle} from 'react-native-svg'; import { View, Text } from 'react-native'; import { formatTime } from '@/utils/formatTime';
export default function TimerRing({remaining,total,label}:{remaining:number;total:number;label:string}){const size=280;const r=120;const c=2*Math.PI*r;const p=total?remaining/total:0;return <View style={{alignItems:'center'}}><Svg width={size} height={size}><Circle cx={size/2} cy={size/2} r={r} stroke='rgba(255,255,255,0.2)' strokeWidth={14} fill='none'/><Circle cx={size/2} cy={size/2} r={r} stroke='#8AA4FF' strokeWidth={14} fill='none' strokeDasharray={`${c*p} ${c}`}/></Svg><View style={{position:'absolute',top:95,alignItems:'center'}}><Text style={{fontSize:56,color:'white'}}>{formatTime(remaining)}</Text><Text style={{color:'white'}}>{label}</Text></View></View>}
